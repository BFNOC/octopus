# utils/xstrings/ - 字符串工具

> 导航：[根目录](../../../CLAUDE.md) > internal > utils > **xstrings**

## 职责

字符串分割、清理工具函数。

## 关键文件

| 文件 | 职责 |
|------|------|
| `xstrings.go` | `SplitTrimCompact(sep, parts...)`：按分隔符分割并去除空白和空项；`TrimCompact(items)`：对字符串切片 trim 并去空 |

## 依赖关系

- 无外部依赖
