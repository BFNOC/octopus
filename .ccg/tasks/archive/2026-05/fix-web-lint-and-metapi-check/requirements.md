# Requirements

用户要求：

- 修复 `web` 下 `pnpm lint` 当前报错。
- 说明之前是否参考了 `/Users/chaos/developments/github_go/metapi` 的设计再修改 Octopus，并用具体依据回答。

约束：

- 不扩大本次 lint 修复范围到无关重构。
- 保留上一轮 quick settings wizard 改动。
- 对 metapi 参考情况需要基于实际代码路径说明，不泛泛回答。
