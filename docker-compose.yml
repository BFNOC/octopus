services:
  octopus:
    image: hureru/octopus
    pull_policy: always
    ports:
       - '8080:8080'
    volumes:      
       - '/path/to/data:/app/data'
    container_name: octopus
    restart: unless-stopped
